# homebrew-tap

wis-graph 도구들의 Homebrew 탭.

```
brew install wis-graph/tap/desklog
```

## desklog

책상 앞에서 무슨 일이 있었는지 구간 단위로 기록하는 CLI 수집기.
→ https://github.com/wis-graph/desklog

```
brew install wis-graph/tap/desklog
brew services start desklog      # 로그인할 때 자동으로 수집을 시작한다
desklog top 7
```
